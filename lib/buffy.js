var TabBarView = require('./tab-bar-view');

process.env.NODE_PATH = "/Applications/Atom.app/Contents/Resources/app/node_modules:" + process.env.NODE_PATH;

module.exports = {
  activate: function (state) {
  }
};
